import { type ReactNode } from 'react';
import Badges from './Badges';

export default function HeroLeftSection(): ReactNode {
    return (
        <section>
            <Badges />
        </section>
    )
}